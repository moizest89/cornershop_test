<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cornershop.counterstest">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".CountersTest"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CountersNormal"
        android:usesCleartextTraffic="true">
        <activity android:name=".presentation.splash.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.counter.add.CounterAddActivity"
            android:label="@string/create_counter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".presentation.counter.list.CounterListActivity"
            android:label="@string/title_activity_counter_list"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".presentation.welcome.WelcomeActivity"
            android:theme="@style/Theme.CountersWelcome"/>
    </application>

</manifest>